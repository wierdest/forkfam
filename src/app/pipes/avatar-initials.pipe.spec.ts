import { AvatarInitialsPipe } from './avatar-initials.pipe';

describe('AvatarInitialsPipe', () => {
  it('create an instance', () => {
    const pipe = new AvatarInitialsPipe();
    expect(pipe).toBeTruthy();
  });
});
